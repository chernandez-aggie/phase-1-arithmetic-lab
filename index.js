//multiply is an equation whose multiplied result will equal 62

const num1 = 2;
const num2 = 31;

let multiply = num1 * num2;

//Generate random integer greater than 0

let random = Math.floor(Math.random()) + 1;

// mod is an equation that calculates a remainder that is equal to 4:

const num3 = 16;
const num4 = 6;

let mod = num3 % num4;

//max will return 20 as the highest number in the set:

let max = Math.max(20, 1, 3, 8, 2, 14);